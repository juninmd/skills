{
  "name": "@luizalabs/padrao-labs-agents",
  "version": "1.0.0",
  "description": "CLI para instalar skills, agents, rules, hooks e workflows do padrao Luizalabs globalmente",
  "type": "module",
  "bin": {
    "padrao-labs-agents": "./dist/index.js"
  },
  "files": [
    "dist/",
    "templates/",
    "agents-bundle/"
  ],
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "build": "tsc --emitDeclarationOnly && node esbuild.config.mjs",
    "bundle-agents": "node scripts/bundle-agents.mjs",
    "prepublishOnly": "npm run build && npm run bundle-agents",
    "postinstall": "node ./dist/index.js cron"
  },
  "engines": {
    "node": ">=18.3.0"
  },
  "publishConfig": {
    "registry": "https://nexus.luizalabs.com/repository/npm/"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "comment-json": "^4.2.3",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.4.0"
  }
}
