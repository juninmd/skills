{
  "name": "plugin-marketplace-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal tools for generating plugins and a marketplace manifest from agent assets.",
  "type": "module",
  "scripts": {
    "build": "pnpm run lint && pnpm run gen:marketplace",
    "gen:marketplace": "node scripts/generate-marketplace.mjs",
    "lint:code": "eslint \"scripts/**/*.mjs\"",
    "lint:yaml": "node scripts/lint-yaml.mjs",
    "lint:shell": "node scripts/lint-shell.mjs",
    "lint:plugins": "node scripts/lint-plugins.mjs",
    "lint:frontmatter:inline": "node scripts/lint-frontmatter-inline.mjs",
    "lint:skills": "node scripts/lint-skills.mjs",
    "lint:agents": "node scripts/lint-agents.mjs",
    "lint": "pnpm run lint:code && pnpm run lint:yaml && pnpm run lint:shell && pnpm run lint:frontmatter:inline && pnpm run lint:skills && pnpm run lint:agents && pnpm run lint:plugins",
    "test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^9.24.0",
    "globals": "^16.0.0",
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.1",
    "zod": "^4.3.6"
  }
}
