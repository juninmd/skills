name: Validate
on:
  pull_request:
  push:
    branches: [main, master]
concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0

      - name: Spell check Markdown
        uses: streetsidesoftware/cspell-action@de2a73e963e7443969755b648a1008f77033c5b2 # v8.4.0
        with:
          files: |
            README.md
            docs/**/*.md
            .agents/AGENTS.md
            .agents/agents/**/*.md
            .agents/prompts/**/*.md
            .agents/skills/**/*.md

      - name: Link check Markdown
        uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411 # v2.8.0
        with:
          args: >-
            --offline
            README.md
            "docs/**/*.md"
            ".agents/AGENTS.md"
            ".agents/agents/**/*.md"
            ".agents/prompts/**/*.md"
            ".agents/skills/**/*.md"

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271
        with:
          version: 10

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm run validate
      - run: pnpm run docs:build

      - name: PR Skill completeness check and diff
        if: github.event_name == 'pull_request'
        run: pnpm run pr:check
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}

      - name: Comment PR with Skill changes
        if: github.event_name == 'pull_request' && (success() || failure())
        run: |
          if [ -f pr-comment.md ]; then
            gh pr comment ${{ github.event.pull_request.number }} -F pr-comment.md
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

