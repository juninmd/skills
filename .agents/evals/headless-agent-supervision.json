{
  "skill_name": "headless-agent-supervision",
  "trigger": {
    "positive": [
      { "prompt": "Keep this headless CLI agent running through token expiry and quota resets", "top_k": 3 },
      { "prompt": "My unattended batch run dies with 429 every few hours", "top_k": 3 },
      { "prompt": "Detect when a long running agent job has silently stalled", "top_k": 3 }
    ],
    "negative": [
      { "prompt": "Design the tool schemas and the loop for a new agent", "owner": "agent-engineering" },
      { "prompt": "Is this rm -rf command safe to run", "owner": "shell-operations" }
    ]
  }
}
