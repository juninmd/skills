id: negative-trigger-001
name: Negative Trigger 1
description: Auto-generated negative-trigger task.
tags:
  - negative-trigger
inputs:
  prompt: "cross-platform mobile"
expected:
  should_trigger: false
graders:
  - type: text
    name: omits-skill-keywords
    config:
      not_contains:
        - "configuration"
        - "gradle"
        - "initialization"
        - "project"

