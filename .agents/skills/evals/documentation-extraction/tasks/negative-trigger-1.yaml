id: negative-trigger-001
name: Negative Trigger 1
description: Generated negative-trigger routing task.
tags:
  - negative-trigger
inputs:
  prompt: "manual content creation (use spec-first-design)"
expected:
  should_trigger: false
graders:
  - type: text
    name: omits-skill-keywords
    config:
      not_contains:
        - "documentation"
        - "extraction"
