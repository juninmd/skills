id: negative-trigger-001
name: Negative Trigger 1
description: Auto-generated negative-trigger task.
tags:
  - negative-trigger
inputs:
  prompt: "trivial code edits"
expected:
  should_trigger: false
graders:
  - type: text
    name: omits-skill-keywords
    config:
      not_contains:
        - "brainstorming"
        - "non-trivial"
        - "planning"
        - "tasks"

