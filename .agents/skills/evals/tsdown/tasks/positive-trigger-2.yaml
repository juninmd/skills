id: positive-trigger-002
name: Positive Trigger 2
description: Auto-generated positive-trigger task.
tags:
  - positive-trigger
inputs:
  prompt: "generating .d.ts files"
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "building"
        - "files"
        - "generating"
        - "libraries"

