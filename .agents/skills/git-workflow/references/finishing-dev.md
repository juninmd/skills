# Delivery workflow moved

Use the installed `finishing-dev` skill for review subagents, verification and pull request creation. Use `git-workflow` for branch operations, conflict recovery and releases. This reference preserves old links without maintaining a second delivery procedure.
